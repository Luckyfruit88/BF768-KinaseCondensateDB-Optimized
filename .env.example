# Optional production-style configuration
# For course demo, config.py includes a sample Team3 database URL.
# For safer deployment, override it here instead of editing code.
SECRET_KEY=change-me
JWT_SECRET=change-me-too
JWT_EXPIRE_HOURS=8
DATABASE_URL=mysql+pymysql://USER:PASSWORD@HOST:PORT/Team3?charset=utf8mb4
